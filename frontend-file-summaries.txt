{
  "frontend/src/index.css": "Global dark terminal stylesheet with OKLCH color tokens for both light/dark modes, JetBrains Mono font, custom scrollbar, and utility classes for buy/sell/warning colors.",
  "frontend/tailwind.config.js": "Extended Tailwind config with JetBrains Mono font, terminal-themed OKLCH color tokens (buy/sell/neutral/warning), custom shadows, and pulse/blink/fade-in animations.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend interactions; `useStartBot` and `useStopBot` now include a `healthCheck` pre-flight call that aborts with a friendly \"not reachable\" error if the canister is unavailable, and wrap the actual mutation call to detect IC0508/reject-code-5 errors and surface a human-readable \"canister is currently stopped\" message instead of raw rejection JSON.",
  "frontend/src/components/BotControlPanel.tsx": "Bot control panel that uses `useStartBot`/`useStopBot` mutation error state directly (instead of local `actionError` state) to display user-friendly error messages with a `WifiOff` icon for reachability errors and `AlertCircle` for stopped-canister errors; buttons return to their default non-loading state automatically when `isError` is true.",
  "frontend/src/components/ConfigurationPanel.tsx": "Configuration panel updated to use the new BotConfig shape (intervalSeconds, spreadBps, numOrders) from useBotConfig, with a read-only note since the backend has no updateConfig method in this version.",
  "frontend/src/components/MarketDataPanel.tsx": "Market data panel rewritten to derive mid price, best bid, and best ask from the last grid data (useLastGrid) since getMarketOverview no longer exists, showing grid level counts and bot status indicator.",
  "frontend/src/components/GridPreviewTable.tsx": "Grid levels panel rewritten to use useLastGrid (calls `getLastGrid()`) instead of the removed getOrderBook, displaying buy/sell price levels with a computed mid-price marker and proper loading/error/empty states.",
  "frontend/src/App.tsx": "Main app layout with header, 3-row responsive dashboard grid (control/config/market, order book/pending orders, trade history/activity log), and footer with caffeine.ai attribution.",
  "frontend/src/components/OpenOrdersPanel.tsx": "Pending orders panel with improved cancellation UX — shows a \"CANCELLING…\" badge in the header, a full overlay spinner with order count while the mutation is in progress (dimming the existing list), a success banner once orders are cleared, and the Cancel All button stays visible and disabled during the operation.",
  "frontend/src/components/TradeHistoryPanel.tsx": "Functional trade history panel using the useTradeHistory hook to display completed/cancelled OrderEntry records in a scrollable table with columns for side (buy/sell colored), price, quantity, status, and relative timestamp; includes loading skeletons, empty state, error state with retry, and a manual refresh button.",
  "frontend/src/components/ActivityLogPanel.tsx": "Activity log panel that uses the useActivityLog hook (count: 50, page: 0) to fetch and display LogEntry records in reverse-chronological order with formatted timestamps and event-type-colored messages, plus loading skeletons, error state, and empty state.",
  "frontend/src/backend.d.ts": "TypeScript interface definitions for the new TradingBot canister, including Quote, Level10, Liquidity, Stats, Info, PendingOrder, OrderResult, MarketOverview, OrderBook, BotConfig types and the backendInterface with all 8 public methods."
}