{
  "frontend/src/index.css": "Global dark terminal stylesheet with OKLCH color tokens for both light/dark modes, JetBrains Mono font, custom scrollbar, and utility classes for buy/sell/warning colors.",
  "frontend/tailwind.config.js": "Extended Tailwind config with JetBrains Mono font, terminal-themed OKLCH color tokens (buy/sell/neutral/warning), custom shadows, and pulse/blink/fade-in animations.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend interactions; added `useBalances` hook that calls `actor.getBalances()` returning `{ icpBalance, ckbtcBalance }` with 30-second polling, and added `Balances` interface export. All other hooks unchanged.",
  "frontend/src/components/BotControlPanel.tsx": "Bot control panel for ckBTC/ICP; when the bot is running, the config summary section is replaced with a 'Loop active – orders syncing' banner featuring a pulsing green indicator and a slow-spinning refresh icon, plus compact stats (spread, orders, size); when stopped, shows the standard spread/orders/interval grid.",
  "frontend/src/components/ConfigurationPanel.tsx": "Configuration panel for ICP/ckBTC grid trading; updated the default `orderSize` from 1 to 5 ICP in the initial form state and updated the placeholder text from \"e.g. 1\" to \"e.g. 5\", so the helper text now reads \"Total exposure: 40 ICP across all 8 levels\" by default.",
  "frontend/src/components/MarketDataPanel.tsx": "Market data panel for ICP/ckBTC pair; updated `formatPrice` to always show 8 decimal places consistent with the ICP/ckBTC price range (~0.00020), added spread display in pips, and updated the no-data message to reference ICP/ckBTC.",
  "frontend/src/components/GridPreviewTable.tsx": "Grid levels panel displaying ICP/ckBTC price levels with per-level order status badges; imports `useOpenOrders` and cross-references each buy/sell grid level against live open orders to show a green 'PLACED' or amber 'MISSING' badge in a new STATUS column, plus a header summary showing how many levels are placed out of total.",
  "frontend/src/App.tsx": "Main app layout with ckBTC/ICP header branding; added `BalancePanel` import and rendered it in a dedicated full-width row between the top control row and the order book row for high visibility.",
  "frontend/src/components/OpenOrdersPanel.tsx": "Open orders panel for ckBTC/ICP; fixed column headers from `PRICE (ckUSDT)` / `QTY (ICP)` to `PRICE (ICP)` / `QTY (ckBTC)`, updated `formatPrice` to always show 4+ decimal places, and updated `formatQty` to always show 8 decimal places for ckBTC satoshi precision.",
  "frontend/src/components/TradeHistoryPanel.tsx": "Trade history panel using useTradeHistory to display completed/cancelled orders in reverse-chronological order; properly distinguishes initial loading (skeletons), error state (with message and retry), and empty state with a polling indicator using fetchStatus.",
  "frontend/src/components/ActivityLogPanel.tsx": "Activity log panel with updated `eventColor` function that detects `[ORDER_ERROR]` tags and failure event types (`buy_order_creation_failed`, `sell_order_creation_failed`, etc.) to render them in destructive red with a highlighted background and ERR badge; all other event type colors preserved.",
  "frontend/src/backend.d.ts": "TypeScript interface definitions for the new TradingBot canister, including Quote, Level10, Liquidity, Stats, Info, PendingOrder, OrderResult, MarketOverview, OrderBook, BotConfig types and the backendInterface with all 8 public methods.",
  "frontend/src/components/BalancePanel.tsx": "New panel component displaying the bot's ICP (4 decimal places) and ckBTC (8 decimal places) balances using the `useBalances` hook; shows amber warning badges and an explanatory note when either balance is zero, with loading skeletons, error state, and a 30s polling indicator."
}